var config = {
    /*
     * Server which the bot should connect
     */
    server: 'irc.freenode.net',
    
    /*
     * Nickname that the bot should use
     */
    nick: 'Spudnik',
    
    /*
     * Channels the bot should join
     */
    channel: '#hacklabmikkeli',
}

module.exports = config;